<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
            http://www.liquibase.org/xml/ns/dbchangelog
            https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
        Act kontrollerlari endi ATTACH_ACT subyektini @PreAuthorize darajasida
        tekshiradi (ilgari faqat isAuthenticated() edi):
          READ    -> GET /v1/acts/mine, /v1/acts/{id}, /pdf, /v1/cards/{id}/acts
          VIEW_ALL-> GET /v1/acts (butun tashkilot bo'yicha ro'yxat)
          ASSIGN  -> POST /v1/cards/{id}/acts
          UPDATE  -> PUT /v1/acts/{id}, PATCH .../ready, POST .../send-to-lis
          DELETE  -> DELETE /v1/acts/{id}

        READ ("o'z aktlarim") va VIEW_ALL ("tashkilotning barcha aktlari")
        kartalardagi USER_INCOMINGS <-> ATTACHED_CARDS ajratishiga o'xshab
        ajratildi. READ/ASSIGN/UPDATE/DELETE 20260825-1200 da seed qilingan;
        bu yerda faqat yangi VIEW_ALL action + uni bo'lim mudiriga berish.
        super_admin/admin uchun zzz-rbac-grants/20260828-1200 runAlways
        CROSS JOIN keyin ishlaydi (VIEW_ALL ni avtomatik oladi).
    -->
    <changeSet id="20260902-1300-01-seed-view-all-action" author="uzinfocom">
        <preConditions onFail="HALT" onError="HALT">
            <dbms type="postgresql"/>
            <tableExists tableName="action"/>
        </preConditions>

        <sql dbms="postgresql" splitStatements="true" stripComments="true"><![CDATA[
            INSERT INTO action (code, active, deleted, version, created_at, updated_at,
                                 description_uz, description_ru, description_uz_cyril, description_kaa)
            VALUES
                ('VIEW_ALL', true, false, 0, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP,
                 'Tashkilotning barcha yozuvlarini ko''rish (nafaqat o''ziniki).',
                 'Просмотр всех записей организации (не только своих).',
                 'Ташкилотнинг барча ёзувларини кўриш (нафақат ўзиники).',
                 'Shólkemniń barlıq jazıwların kóriw (tek ózderiniki emes).')
            ON CONFLICT (code) DO NOTHING;
        ]]></sql>
    </changeSet>

    <changeSet id="20260902-1300-02-grant-epidim-head-view-all" author="uzinfocom">
        <preConditions onFail="HALT" onError="HALT">
            <dbms type="postgresql"/>
            <tableExists tableName="role_permissions"/>
            <tableExists tableName="role_permission_actions"/>
        </preConditions>

        <sql dbms="postgresql" splitStatements="true" stripComments="true"><![CDATA[
            -- Bo'lim mudiri (epidim_head) -> ATTACH_ACT: + VIEW_ALL
            INSERT INTO role_permission_actions (role_permission_id, action_id)
            SELECT rp.id, a.id
            FROM role_permissions rp
            JOIN role r ON r.id = rp.role_id
            JOIN permission p ON p.id = rp.permission_id
            JOIN action a ON a.code = 'VIEW_ALL'
            WHERE r.name = 'isemid_epidim_head' AND p.subject = 'ATTACH_ACT'
            ON CONFLICT (role_permission_id, action_id) DO NOTHING;
        ]]></sql>
    </changeSet>

</databaseChangeLog>
