<?xml version="1.0" encoding="UTF-8"?>

<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
            http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
        Form0581 counterpart of 20260818-1000-seed-notification-settings-affiliated.xml:
        an organization that is neither sender nor receiver, but is the
        patient's workplace/place of study, is notified when a Form0581 is
        created and again when it reaches CARD_LINKED (their cue to attach
        an Act). See
        NotificationEventListener.handleForm0581Received/handleForm0581CardLinked.

        Same WHERE NOT EXISTS guard, same ACTIVE/'true' seeding as the
        original changeset - a no-op for behavior since resolveBoolean
        already defaults an unseeded key to enabled.
    -->
    <changeSet id="20260818-1100-01-seed-notification-settings-form0581-affiliated" author="uzinfocom">
        <sql>
            INSERT INTO system_settings (created_at, updated_at, setting_key, setting_value, value_type, description, active, deleted, version)
            SELECT now(), now(), 'notification.form0581-affiliated-received.enabled', 'true', 'BOOLEAN', 'Notify every organization affiliated with the patient (workplace/place of study, excluding sender/receiver) when a Form0581 is created.', true, false, 0
            WHERE NOT EXISTS (SELECT 1 FROM system_settings WHERE setting_key = 'notification.form0581-affiliated-received.enabled');

            INSERT INTO system_settings (created_at, updated_at, setting_key, setting_value, value_type, description, active, deleted, version)
            SELECT now(), now(), 'notification.form0581-affiliated-card-linked.enabled', 'true', 'BOOLEAN', 'Notify every organization affiliated with the patient (workplace/place of study, excluding sender/receiver) when a Form0581 reaches CARD_LINKED, so they know they can attach an Act.', true, false, 0
            WHERE NOT EXISTS (SELECT 1 FROM system_settings WHERE setting_key = 'notification.form0581-affiliated-card-linked.enabled');
        </sql>
        <rollback>
            <sql>
                DELETE FROM system_settings WHERE setting_key IN (
                    'notification.form0581-affiliated-received.enabled',
                    'notification.form0581-affiliated-card-linked.enabled'
                );
            </sql>
        </rollback>
    </changeSet>

</databaseChangeLog>
